# ── Compose configuration — copy to .env and adjust ─────────────────────────
#   cp .env.docker.example .env
# .env is gitignored; never commit real secrets.

# Postgres credentials (the api builds its DATABASE_URL from these).
POSTGRES_USER=tia
POSTGRES_PASSWORD=change-me-in-prod
POSTGRES_DB=tia

# Host port the SPA (nginx) is published on → http://localhost:${WEB_PORT}
WEB_PORT=8080

# API runtime
UVICORN_WORKERS=2
LOG_LEVEL=info
TIA_SEED_ON_START=true
TIA_SYNTH_ON_START=true

# GLM-OCR (Modal). Leave the key empty to disable the handwritten/vision case.
GLM_OCR_BASE_URL=https://versifine--glm-ocr-serve.modal.run
GLM_OCR_API_KEY=

# Shell scripts must stay LF-terminated so they run inside Linux containers
# regardless of the host (Windows checkouts otherwise corrupt them with CRLF).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
